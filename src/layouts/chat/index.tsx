import React, { useState } from 'react'
import { styled } from 'styled-components'
import {} from 'antd'

import RootLayout from '@/components/RootLayout'

function Chat() {
    const [collapsed, setCollapsed] = useState(false)
    return <RootLayout>RootLayout</RootLayout>
}

export default Chat
