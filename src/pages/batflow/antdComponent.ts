import { Input, InputNumber, Button, Radio, Table, DatePicker, Checkbox } from 'antd'

const antdComponent: any = {
    Input,
    InputNumber
    // Button,
    // Table,
    // DatePicker,
    // Select,
    // Checkbox: Checkbox.Group,
    // UploadImage: Upload,
    // UploadFile: Upload,
    // TextArea: Input.TextArea,
}

export default antdComponent
